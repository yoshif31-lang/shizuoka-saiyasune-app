name: GENESIS Universal Patrol
on:
  schedule:
    - cron: '0 0,7 * * *' # 毎日 09:00, 16:00 (JST)
  workflow_dispatch:      # 手動実行ボタン付き

jobs:
  patrol:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install Dependencies
        run: npm install
      - name: Start Scout
        env:
          SUPABASE_URL: ${{ https://rfegjvfthzaflfrhsjmb.supabase.co }}
          SUPABASE_KEY: ${{ sb_publishable_9jkx_z4SgbTk0_rH48adsQ_Q1piP70N }}
        run: node index.js

