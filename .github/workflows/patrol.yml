name: GENESIS OMNI PATROL
on:
  workflow_dispatch:

jobs:
  launch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install @supabase/supabase-js axios cheerio
      - name: Execute OMNI-ENGINE
        env:
          SUPABASE_URL: ${{https://rfegjvfthzaflfrhsjmb.supabase.co}}
          SUPABASE_KEY: ${{sb_publishable_9jkx_z4SgbTk0_rH48adsQ_Q1piP70N}}
        run: node index.js

